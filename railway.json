{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build:railway && cd server && npm install && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd server && node index.js"
  }
}
