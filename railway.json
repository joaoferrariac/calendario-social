{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Skip frontend build' && cd server && npm install && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd server && node index.js"
  }
}
